# 当前工作状态

更新时间：{{updated_at}}
当前活动任务：无

## 接手导航

1. 先读本文件，判断是否有当前活动任务和任务卡。
2. 如有当前活动任务，读取对应 `.Ai-config/tasks/*.md`。
3. 按任务类型读取项目事实源；项目级 skill 规则从 `.Ai-config/skills/<skill-name>/` 开始。
4. README / 运维文档 / 架构文档只在需要时再读，不要把它们复制进本文件。

## 当前活动任务

- 无。

## 待用户确认

- 暂无。

## 暂停 / 阻塞

- 暂无。

## 最近关闭

- 暂无。

## 接手规则

- 简单问答、一次性命令、一轮内完成且无残留风险的小修复，不创建任务卡。
- 跨天、中断、切换、阻塞、等待确认或有残留风险时，再创建或更新任务卡。
- 切换任务前先保存当前任务状态，不要覆盖旧任务卡。
- 未确认、未验证或有残留风险的任务不要直接关闭。

## 形态约束与 hygiene（不要删除本节）

本文件是接手入口和状态总览，不是存储器。每次更新本文件前先 hygiene，再写本轮变化。

### 硬上限

- 整文件建议 ≤ 120 行或 ≤ 6KB。
- 当前活动任务 ≤ 3 条，每条 ≤ 4 行；详情只在任务卡。
- 待用户确认 ≤ 5 条，每条必须带日期。
- 暂停 / 阻塞 ≤ 5 条。
- 最近关闭 ≤ 5 条，每条一行摘要。

### 禁止写入本文件

- 静态项目结构、目录地图、技术栈百科 → README / 架构文档，最多一行链接。
- 本地/服务器启动命令、端口、路径 runbook → ops 文档或项目 skill，最多一行链接。
- BuildId、SHA、备份账本、验收日志正文 → 任务卡或部署文档，最多一行链接。
- changelog / 已完成长文 → CHANGELOG 或已关闭任务卡。
- 风险分级、验证分级、完整规则复述 → 只允许一行引用来源。

### 维护动作

- 关闭任务卡后，必须从「当前活动 / 待确认 / 暂停阻塞」移除对应行。
- 待确认超过 14 天无人跟进：下次更新时催关闭，或移到「最近关闭」并注明「未跟进关闭」。
- 溢出的关闭记录不要堆在本文件；保留任务卡即可。
- 用户要求「更新项目 AI 配置」时，把本文件健康检查作为 repair 必做项，而不是继续追加。
