# 当前工作状态

更新时间：{{updated_at}}
当前活动任务：无

## 接手导航

1. 先读本文件，判断是否有当前活动任务和任务卡。
2. 再按需读 `.Ai-config/README.md`，理解当前项目 AI 协作中枢边界。
3. 如果有当前活动任务，读取对应 `.Ai-config/tasks/*.md`。
4. 按任务类型读取当前项目的真实事实源、架构文档、配置文件或脚本；如果任务涉及项目级 skill 规则，先从 `.Ai-config/skills/<skill-name>/` canonical 事实源开始，其他文档只作为证据或背景。
5. 项目主 README 只作为项目概览补充，除非当前项目明确把它定义为接手入口。

## 当前活动任务

- 无。

## 待用户确认

- 暂无。

## 暂停 / 阻塞

- 暂无。

## 最近关闭

- 暂无。

## 接手规则

- 简单问答、一次性命令、一轮内完成且无残留风险的小修复，不需要创建任务卡。
- 任务跨天、中断、切换、阻塞、等待确认，或有残留风险时，再创建或更新 `.Ai-config/tasks/*.md`。
- 切换任务前，先保存当前任务状态，不要覆盖旧任务卡。
- 未确认、未验证或有残留风险的任务不要直接关闭，应保持为待用户确认、等待验证、暂停或阻塞。
- 任务关闭时应记录结果、验证情况、残留风险和关闭依据。

## 形态约束（不要删除本节）

- 本文件是接手入口和状态总览，不是项目地图或规则复述。以下内容不允许写入本文件，只能链接到其他文档：
  - 静态项目结构、目录说明、模块地图 → 放 README 或架构文档，本文件最多用一行链接指向。
  - 任何风险分级、验证分级或触发规则的完整复述 → 只允许一行引用来源文件，不允许照抄条目；来源规则改了，这里不需要跟着改，因为这里本来就不该有复述内容。
- "当前待确认"每条记录必须带日期；超过 30 天无人跟进的条目，下次更新本文件时移到"最近关闭"并注明"未跟进关闭"，不允许无限期留在待确认表里。
- 关闭 `.Ai-config/tasks/*.md` 中的任务卡后，必须同步从本文件"当前待确认"或"暂停 / 阻塞"中移除对应行，两处状态不允许长期不一致。
