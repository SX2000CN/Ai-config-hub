{
  "name": "@ai-config-hub/local-webfetch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local MCP server that fetches URLs from this machine's own network (respects HTTP_PROXY/HTTPS_PROXY and VPN routing), unlike the cloud-side WebFetch tool.",
  "main": "index.js",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "turndown": "^7.2.4",
    "undici": "^8.7.0",
    "zod": "^3.24.0"
  }
}
